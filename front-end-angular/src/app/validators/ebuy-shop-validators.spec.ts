import { EBuyShopValidators } from './ebuy-shop-validators';

describe('EBuyShopValidators', () => {
  it('should create an instance', () => {
    expect(new EBuyShopValidators()).toBeTruthy();
  });
});
